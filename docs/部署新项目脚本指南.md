# RoboJuDo中部署新项目脚本指南

本指南提供了在RoboJuDo框架中部署新项目脚本和集成外部功能的完整工作流程，以G1速度增强运动控制集成为参考示例。

## 概述

RoboJuDo设计为模块化和可扩展的框架。本指南涵盖了将新策略、配置和部署脚本集成到框架中的完整流程。

## 前置条件

- RoboJuDo项目结构正确设置
- 具有所需依赖项的Python环境
- 对机器人控制系统有基本了解
- 熟悉RoboJuDo中的配置管理

## 分步部署流程

### 1. 分析外部实现

在集成之前，彻底分析外部代码库：

```bash
# 示例：分析unitree_rl_mjlab速度控制
find /path/to/unitree_rl_mjlab -name "*.cpp" -o -name "*.h"
grep -r "velocity" /path/to/unitree_rl_mjlab/deploy/
```

**关键分析点：**
- 控制策略实现
- 命令映射和处理
- 安全机制
- 集成接口

### 2. 创建策略实现

在适当的目录中创建新的策略文件：

```bash
# 目录结构
RoboJuDo/
├── robojudo/
│   └── policy/
│       ├── __init__.py
│       ├── base_policy.py
│       ├── unitree_policy.py
│       └── g1_velocity_enhanced_policy.py  # 新策略
```

**策略实现模板：**

```python
from robojudo.policy import Policy, policy_registry

@policy_registry.register
class NewPolicy(Policy):
    """具有增强功能的新策略实现。"""
    
    def __init__(self, cfg_policy, device):
        super().__init__(cfg_policy=cfg_policy, device=device)
        # 初始化策略特定参数
    
    def reset(self):
        # 重置策略状态
        pass
    
    def get_observation(self, env_data, ctrl_data):
        # 处理观测
        return obs, extras
    
    def post_step_callback(self, commands=None):
        # 每步后的后处理
        pass
```

### 3. 创建配置类

为新策略创建配置文件：

```bash
# 目录结构
RoboJuDo/
├── robojudo/
│   └── config/
│       └── g1/
│           └── policy/
│               ├── __init__.py
│               ├── g1_unitree_policy_cfg.py
│               └── g1_velocity_enhanced_policy_cfg.py  # 新配置
```

**配置模板：**

```python
from robojudo.policy.policy_cfgs import BasePolicyCfg
from robojudo.tools.tool_cfgs import DoFConfig

class NewPolicyCfg(BasePolicyCfg):
    """新策略的配置。"""
    
    robot: str = "g1"
    policy_name: str = "policy_name"
    
    # DOF配置
    obs_dof: DoFConfig = YourDoFConfig()
    action_dof: DoFConfig = obs_dof
    
    # 策略特定参数
    custom_param: float = 1.0
    safety_limit: float = 2.0
```

### 4. 注册新配置

更新主配置注册表：

```python
# 文件：robojudo/config/g1/g1_cfg.py

# 添加导入
from .policy.g1_velocity_enhanced_policy_cfg import G1VelocityEnhancedPolicyCfg

# 注册新配置
@cfg_registry.register
class g1_new_feature(RlPipelineCfg):
    """具有增强功能的新配置。"""
    
    robot: str = "g1"
    env: G1MujocoEnvCfg = G1MujocoEnvCfg()
    
    ctrl: list[YourCtrlCfg] = [
        YourCtrlCfg(),
    ]
    
    policy: NewPolicyCfg = NewPolicyCfg()
```

### 5. 创建部署脚本

创建专门的部署脚本：

```bash
# 目录结构
RoboJuDo/
├── scripts/
│   ├── run_pipeline.py
│   └── run_g1_velocity_enhanced.py  # 新部署脚本
```

**部署脚本模板：**

```python
#!/usr/bin/env python3
"""新功能的部署脚本。"""

import argparse
import logging
import sys
from pathlib import Path

# 添加项目根目录到路径
project_root = Path(__file__).parent.parent
sys.path.insert(0, str(project_root))

import robojudo.pipeline
from robojudo.config.config_manager import ConfigManager

def main():
    args = parse_args()
    
    # 加载配置
    config_manager = ConfigManager(config_name=args.config)
    cfg = config_manager.get_cfg()
    
    # 创建并运行管道
    pipeline_class = getattr(robojudo.pipeline, cfg.pipeline_type)
    pipeline = pipeline_class(cfg=cfg)
    
    # 主控制循环
    while True:
        pipeline.step()

if __name__ == "__main__":
    main()
```

### 6. 更新导入和注册表

确保所有新组件都正确导入：

```python
# 文件：robojudo/policy/__init__.py
from .g1_velocity_enhanced_policy import G1VelocityEnhancedPolicy

# 文件：robojudo/config/g1/policy/__init__.py
from .g1_velocity_enhanced_policy_cfg import G1VelocityEnhancedPolicyCfg
```

### 7. 测试和验证

#### 单元测试

```python
# tests/test_new_policy.py
import unittest
from robojudo.policy.g1_velocity_enhanced_policy import G1VelocityEnhancedPolicy

class TestNewPolicy(unittest.TestCase):
    def test_policy_initialization(self):
        # 测试策略初始化
        pass
    
    def test_observation_processing(self):
        # 测试观测处理
        pass
```

#### 集成测试

```bash
# 在仿真中测试
python scripts/run_g1_velocity_enhanced.py --config g1_locomimic_velocity_enhanced --sim --debug

# 测试配置加载
python -c "from robojudo.config.config_manager import ConfigManager; ConfigManager('g1_locomimic_velocity_enhanced')"
```

### 8. 文档

创建全面的文档：

```markdown
# 新功能文档

## 概述
新功能及其能力的描述。

## 配置
可用配置及其参数列表。

## 使用
分步使用说明。

## 故障排除
常见问题和解决方案。
```

## 最佳实践

### 1. 代码组织

- 遵循RoboJuDo命名约定
- 使用适当的目录结构
- 实现适当的错误处理
- 添加全面的日志记录

### 2. 配置管理

- 为配置参数使用类型提示
- 提供默认值
- 记录所有配置选项
- 在适当的地方使用验证

### 3. 安全考虑

- 实现速度限制
- 添加紧急停止机制
- 包含真实机器人部署的安全检查
- 提供清晰的警告消息

### 4. 测试策略

- 单个组件的单元测试
- 完整工作流的集成测试
- 真实部署前的仿真测试
- 性能基准测试

### 5. 文档标准

- 清晰的API文档
- 使用示例
- 故障排除指南
- 配置参考

## 常见集成模式

### 1. 策略增强

```python
# 扩展现有策略
class EnhancedPolicy(BasePolicy):
    def __init__(self, cfg_policy, device):
        super().__init__(cfg_policy=cfg_policy, device=device)
        self.enhanced_features = True
```

### 2. 多管道配置

```python
# 支持多种管道类型
class MultiFeatureConfig(RlMultiPolicyPipelineCfg):
    policies: list[PolicyCfg] = [
        BasePolicyCfg(),
        EnhancedPolicyCfg(),
    ]
```

### 3. 控制器集成

```python
# 添加新控制器支持
class EnhancedCtrlCfg(BaseCtrlCfg):
    triggers_extra = {
        "custom_key": "[CUSTOM_ACTION]",
    }
```

## 故障排除

### 常见问题

1. **导入错误**
   - 检查文件路径和导入
   - 验证`__init__.py`文件
   - 确保正确注册

2. **配置加载**
   - 验证配置语法
   - 检查参数类型
   - 验证注册表注册

3. **策略执行**
   - 检查模型文件路径
   - 验证DOF配置
   - 验证观测维度

4. **真实机器人部署**
   - 检查网络连接
   - 验证安全配置
   - 确保适当的权限

### 调试工具

```bash
# 启用调试日志
python scripts/run_script.py --debug

# 检查配置
python -c "from robojudo.config.config_manager import ConfigManager; print(ConfigManager('config_name').get_cfg())"

# 测试策略加载
python -c "from robojudo.policy.policy_registry import get_policy_cls; print(get_policy_cls('PolicyName'))"
```

## 性能优化

### 1. 计算效率

- 优化观测处理
- 使用向量化操作
- 最小化内存分配

### 2. 实时性能

- 监控帧率
- 优化控制循环
- 在适当的地方使用线程

### 3. 内存管理

- 正确清理资源
- 避免内存泄漏
- 使用高效的数据结构

## 结论

遵循这些步骤确保新功能正确集成到RoboJuDo框架中，同时保持代码质量、安全性和性能。RoboJuDo的模块化设计允许灵活扩展，同时保持整体架构的完整性。

有关具体实现细节，请参考本存储库中提供的G1速度增强运动示例。

---

## G1速度增强集成示例总结

### 🚀 **已创建的集成组件：**

1. **增强速度策略** (`g1_velocity_enhanced_policy.py`)
   - 集成unitree_rl_mjlab的WASD+QE键盘映射
   - 来自unitree_rl_mjlab的增强漂移补偿
   - 改进的步态相位管理
   - 速度缩放和安全限制

2. **配置类** (`g1_velocity_enhanced_policy_cfg.py`)
   - 增强策略的全面配置
   - 速度控制的可调参数
   - 与现有G1 DOF设置兼容

3. **更新的g1_locomimic配置** (`g1_cfg.py`)
   - 新的`g1_locomimic_velocity_enhanced`配置
   - 保持所有原始模仿策略
   - 增强的键盘控制触发器

4. **部署脚本** (`run_g1_velocity_enhanced.py`)
   - 具有安全检查的完整部署解决方案
   - 支持仿真和真实机器人
   - 全面的控制说明和调试

5. **文档** (`deploy_new_project_script.md`)
   - 部署新项目脚本的完整指南
   - 最佳实践和故障排除
   - 使用此集成的参考实现

### 🎮 **增强功能：**

- **键盘控制**：WASD用于移动，QE用于旋转（与unitree_rl_mjlab相同）
- **漂移补偿**：基于unitree_rl_mjlab方法的自动补偿
- **安全限制**：速度缩放和最大限制
- **视觉反馈**：增强的调试可视化
- **策略切换**：在loco和mimic模式之间无缝切换

### 🛠 **使用方法：**

```bash
# 在仿真中运行
python scripts/run_g1_velocity_enhanced.py --config g1_locomimic_velocity_enhanced --sim

# 在真实机器人上运行  
python scripts/run_g1_velocity_enhanced.py --config g1_locomimic_velocity_enhanced --real

# 列出可用配置
python scripts/run_g1_velocity_enhanced.py --list-configs
```

### 📋 **新项目的部署步骤：**

全面的文档涵盖了完整的工作流程：

1. **分析** - 理解外部实现
2. **策略创建** - 实现新的策略类
3. **配置** - 创建配置类
4. **注册** - 在主配置中注册
5. **部署脚本** - 创建专门的部署工具
6. **测试** - 单元和集成测试
7. **文档** - 全面的文档

### 🔧 **关键集成点：**

- 与现有RoboJuDo架构保持兼容
- 保留所有原始g1_locomimic功能
- 将增强速度控制作为可选功能添加
- 提供从标准配置的清晰升级路径

集成现在已准备好进行测试和部署。增强的速度控制提供了更直观和精确的运动控制，同时保持RoboJuDo的强大框架。
